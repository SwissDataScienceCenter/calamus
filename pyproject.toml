[tool.poetry]
name = "marshld"
version = "0.1.0"
description = ""
authors = ["Panaetius <ralf.grubenmann@sdsc.ethz.ch>"]

[tool.poetry.dependencies]
python = "^3.8"
marshmallow = "^3.5.1"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
